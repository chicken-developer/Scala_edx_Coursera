package Scenes

class MainMenuScene{
  object MainMenuScene{
    def main(args: Array[String]): Unit ={
      print("This is MainMenu Scene")
    }
    /*
    This game have a scene, for each scene have layer, in layer have label and button
     */
  }
}